import * as assert from "assert";
import {
  defaultIsMetaFile,
  defaultShouldIncludeFile,
  getJson,
  getJsonPromise,
  getStatData,
  isJsonFile,
  linkAdder,
  pureAssign,
  readFileContents,
} from "./utils";

describe("utils", () => {
  describe("defaultIsMetaFile", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("defaultShouldIncludeFile", () => {
    it("should be a function", () => {
      assert.equal(true, true);
    });
  });

  describe("getJson", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("getJsonPromise", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("getStatData", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("isJsonFile", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("linkAdder", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("pureAssign", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("readFileContents", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });
});
