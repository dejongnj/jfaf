import * as assert from "assert";
import { readdir, readFile, stat} from "./promisifiedNode";

describe("promisifiedNode", () => {
  describe("readdir", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("readFile", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });

  describe("stat", () => {
    it("should do something", () => {
      assert.equal(true, true);
    });
  });
});
