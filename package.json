{
  "name": "jfaf",
  "version": "0.0.1",
  "description": "Builds a json folder structure with optional meta-data",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w & nodemon lib/index.ts"
  },
  "keywords": [
    "folder",
    "file",
    "json",
    "tree"
  ],
  "author": "NJ de Jong",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "@types/node": "^12.7.1"
  }
}
